{
  "name": "@gridhealth/shared",
  "version": "1.0.0",
  "description": "Shared JSON Schema files for GridHealth",
  "main": "schemas/index.json",
  "scripts": {
    "build": "echo 'JSON Schema files are ready to use'",
    "dev": "echo 'JSON Schema files are ready to use'",
    "clean": "echo 'No build artifacts to clean'",
    "test": "echo 'No tests configured for schemas'",
    "lint": "echo 'No linting configured for schemas'",
    "validate-schemas": "ajv validate -s schemas/organization.json -d test-data/organization.json"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  }
} 